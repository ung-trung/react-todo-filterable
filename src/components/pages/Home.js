import React from 'react';
import TodoList from '../todos/TodoList';

const Home = () => {
  return (
    <>
      <section className="section">
        <TodoList />
      </section>
    </>
  );
};

export default Home;
